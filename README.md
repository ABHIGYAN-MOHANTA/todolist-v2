# Todolist-v2
write "npm install" in your cloned repository to install node modules
to run write "node app.js" in the cloned repository
Enjoy

#Features
This version 2.0 is dynamic,implemented using express routing you can create unlimited number of lists.
Also, MongoDB is used as database to store list items.
You can delete list items by checking the checkbox.
